{
  "name": "components-library-seb",
  "scripts": {
    "graph": "npx nx graph",
    "test": "lerna run test",
    "build": "lerna run build",
    "version": "lerna version",
    "postinstall": "lerna run build",
    "publish": "lerna run build && lerna run tsc && lerna publish --force-publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.1.3"
  }
}
