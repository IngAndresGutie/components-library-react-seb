{
  "name": "components-library-seb",
  "version": "0.0.12-alpha.0",
  "private": false,
  "scripts": {
    "graph": "npx nx graph",
    "test": "lerna run test",
    "publish": "lerna publish"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^7.1.3"
  }
}
